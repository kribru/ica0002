---
  - name: Init
    hosts: all
    become: yes
    roles:
      - init
      - backup

  - name: Node exporter
    hosts: all
    become: yes
    roles:
      - node_exporter

  - name: DNS server
    hosts: dns_servers
    become: yes
    roles:
      - bind
      - bind_exporter

  - name: DNS records
    hosts: dns_master
    become: yes
    roles:
      - dns_records

  - name: install nginx to servers
    hosts: all
    become: yes
    roles:
      - nginx
      - nginx_exporter

  - name: Prometheus
    hosts: prometheus
    become: yes
    roles:
      - prometheus

  - name: Database server
    hosts: db_servers
    become: yes
    roles: 
      - mysql
      - mysql_exporter
      - mysql_backup

  - name: Web server
    hosts: web_servers
    become: yes 
    roles:
      - iptables
      - docker
      - agama

  - name: Grafana
    hosts: grafana
    become: yes
    roles:
      - docker
      - grafana

  - name: Influxdb
    hosts: influxdb
    become: yes
    roles:
      - influxdb
      - pinger
      - influxdb_exporter
      - influxdb_backup

  - name: Rsyslog
    hosts: influxdb
    become: yes
    roles:
      - rsyslog

  - name: Keepalived, haproxy
    hosts: haproxy
    become: yes
    roles:
      - keepalived
      - haproxy
      - haproxy_exporter
      - keepalived_exporter
